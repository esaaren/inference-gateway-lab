apiVersion: inference.networking.x-k8s.io/v1alpha2 # Added .x- and updated to v1alpha2
kind: InferenceObjective
metadata:
  name: food-review
spec:
  priority: 10
  poolRef:
    group: "inference.networking.k8s.io" # Pools stay in the main group
    kind: "InferencePool"
    name: vllm-llama3-1-8b-instruct
---
apiVersion: inference.networking.x-k8s.io/v1alpha2 # Added .x- and updated to v1alpha2
kind: InferenceObjective
metadata:
  name: llama3-base-model
spec:
  priority: 20
  poolRef:
    group: "inference.networking.k8s.io" # Pools stay in the main group
    kind: "InferencePool"
    name: vllm-llama3-1-8b-instruct